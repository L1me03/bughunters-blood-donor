<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AplikacijaDonorApp2.Views.SplashPage"
             BackgroundColor="#FCA7A7"
             NavigationPage.HasNavigationBar="False">

    <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
        <Grid>
            <Label Text="WELCOME"
               FontSize="40"
               FontFamily="Jomolhari"
               FontAttributes="Bold"
               TextColor="DarkRed"
               HorizontalOptions="Center"
               VerticalOptions="Center"/>
        </Grid>
        <Label Text="Every drop counts. Every donor is a life-saver." FontFamily="GreatVibes" FontSize="16" Margin="0,10"/>
        <Image Source="drop_character.png" HeightRequest="150" Margin="0,20"/>
        <Label Text="BugHunters ©"
       FontSize="12"
       VerticalOptions="End"
       HorizontalOptions="Center"
       Margin="0,20"/>

    </StackLayout>
</ContentPage>

